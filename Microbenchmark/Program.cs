using System;
using System.Collections.Generic;
using System.Diagnostics;
using Tokenizer;

namespace Microbenchmark
{
    class Program
    {
        static void Main( string[] args )
        {
            //Stopwatch hashetWatch = new Stopwatch();
            //Stopwatch scanWatch = new Stopwatch();
            //Random random = new Random();
            //byte[] buffer = new byte[50_000_000];
            //random.NextBytes( buffer );
            //for( int i = 0; i < buffer.Length; i++ )
            //{
            //    byte curr = buffer[i];
            //    hashetWatch.Start();
            //    bool isToken = hashset.Contains( (SingleCharToken)curr );
            //    hashetWatch.Stop();
            //    scanWatch.Start();
            //    bool isToken2 = ((SingleCharToken)curr).IsSingleCharToken();
            //    scanWatch.Stop();
            //    if(isToken != isToken2)
            //    {

            //    }
            //}

        }
    }
}
